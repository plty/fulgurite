---
import { type Lang } from "$components/editor/lang-support";
import { VeryStaticBlox } from "$components/VeryStaticBlox";

interface Props {
  content: string;
  language: Lang;
}

const { content, language } = Astro.props;
---

<dev class="no-prose text-sm">
  <VeryStaticBlox code={content} lang={language} lineGroup={{}} />
</dev>
